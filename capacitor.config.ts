import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.fotosgramck',
  appName: 'fotosgram',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
